# Utilities

### encodeHex()

**Arguments**

1. `data: string | Array<number>`
1. `type?: string = 'utf8'`

**Returns** `hex`.

### decodeHex()

**Arguments**

1. `data: hex`
1. `type?: string = 'utf8'`

**Returns** `string` encoded based on `type`.
